# cremi-deform
Introduce distortions to labeled volumes

## Build instructions:

Build it with

```bash
mvn clean install
```

Now you can build a fat jar with

```bash
mvn clean package
```

to be found in `targets`.
